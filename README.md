# FeeCalculator Service
Uses C# 14 and .NET 10.

A Swagger UI is available at https://localhost:7141/swagger when the service is running.

Run from Visual Studio or use the command line:
>FeeCalculatorService dotnet run

Test from Visual Studio or use the command line:
>FeeCalculatorService.UnitTests dotnet test
